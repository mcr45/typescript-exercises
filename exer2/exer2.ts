/* Declare an array named subjects containing strings.
Declare a tuple named student that contains a string (for the name) and a number (for the age). */

let subjects:string[]=['john','dave','gue']
let student:[string,number]
student=['john',8]
